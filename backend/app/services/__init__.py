"""Service layer — orchestration logic lives here.

Available service modules:
    - ``map_service``: City / road-network loading via OSM
    - ``demand_service``: NPC trip demand generation
    - ``balance_service``: Rebalancing analysis and dispatch
    - ``engine_manager``: SimulationEngine lifecycle management
"""
